<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             x:Class="XamarinBase.MainPage">
    <ContentPage.Behaviors>
        <xct:EventToCommandBehavior EventName="Appearing" Command="{Binding GetPlantsCmd}"></xct:EventToCommandBehavior>
    </ContentPage.Behaviors>

    <StackLayout>
        <Label Text="this is a mainpage"></Label>
        <StackLayout BindableLayout.ItemsSource="{Binding PlantViewModels}">
            <BindableLayout.ItemTemplate>
                <DataTemplate>
                    <Grid RowDefinitions="*,*">
                        <Label Grid.Row="0" Text="{Binding Plant.PlantId,StringFormat='The Tree ID {0}'}"></Label>
                        <Image Grid.Row="1" Source="{Binding Image}"></Image>
                    </Grid>
                </DataTemplate>
            </BindableLayout.ItemTemplate>
        </StackLayout>
    </StackLayout>

</ContentPage>
